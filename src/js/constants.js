export const ROWS = 20;
export const COLS = 10;
export const CUBE_SIZE = 40;
export const GAME_BACKGROUND = document.querySelector("#gameBackdrop");
export const CONTEXT = GAME_BACKGROUND.getContext("2d");
export const GAME_WIDTH = GAME_BACKGROUND.width; // this is what we assigned in the html file for our canvas
export const GAME_HEIGHT = GAME_BACKGROUND.height;
export const SCORE_TEXT = document.querySelector("#scoreDiv");
